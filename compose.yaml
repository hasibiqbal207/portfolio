services:
  portfolio-dev:
    build:
      context: .
      dockerfile: docker/Dockerfile.dev
    image: portfolio-dev
    ports:
      - "6000:6000"
    volumes:
      - ./:/app
      - /app/node_modules/

  portfolio-prod:
    build:
      context: .
      dockerfile: docker/Dockerfile.prod
    image: portfolio-prod
    ports:
      - "6000:6000"
    environment:
      NODE_ENV: production
    restart: always
